## 1. 最近记录清理基础

- [x] 1.1 将现有文件缺失错误判断调整为文件和目录均可复用的路径缺失判断，并保持外部删除文档的现有行为。
- [x] 1.2 在编辑器 Store 中实现最小的最近项清理与持久化逻辑：按类型过滤目标路径，并在移除当前上次工作区时清空 `lastWorkspace`。

## 2. 打开失败处理

- [x] 2.1 更新最近文件打开流程：路径不存在时持久化清理结果并显示友好提示，其他错误继续保留记录并显示常规错误。
- [x] 2.2 更新最近工作区打开流程：路径不存在时持久化清理结果、按需清空 `lastWorkspace` 并显示友好提示，其他错误继续保留记录并显示常规错误。

## 3. 行为核对

- [x] 3.1 核对成功打开文件或工作区仍按原有顺序写入最近记录，且实现未增加启动时全量路径检查、新 IPC 通道或新依赖。
